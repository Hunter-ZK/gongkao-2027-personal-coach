import React from 'react';
import {Play,Pause,EyeOff,Eye,HelpCircle,Calendar,Sparkles,Menu} from 'lucide-react';
import {useApp} from '../../context/AppContext';
import {tZen} from '../../utils/zen';

export const Header:React.FC<{onOpenShortcuts:()=>void;onOpenSidebar?:()=>void}>=({onOpenShortcuts,onOpenSidebar})=>{
  const{exams,timerState,pauseTimer,resumeTimer,setActiveTab,zenMode,toggleZenMode}=useApp();const[gd,national]=exams;const fmt=(sec:number)=>`${Math.floor(sec/60).toString().padStart(2,'0')}:${(sec%60).toString().padStart(2,'0')}`;const openAi=()=>window.dispatchEvent(new CustomEvent('gongkao:open-ai'));
  return <header id="app-header" className="h-16 bg-white border-b border-stone-200/90 px-4 md:px-6 flex items-center justify-between sticky top-0 z-30 select-none shadow-[0_1px_3px_rgba(0,0,0,0.02)]"><div className="flex items-center gap-3 md:gap-5">{onOpenSidebar&&<button onClick={onOpenSidebar} className="md:hidden p-2 -ml-1 text-stone-600 hover:text-stone-900 rounded-lg hover:bg-stone-100"><Menu className="w-5 h-5"/></button>}<div onClick={()=>setActiveTab('dashboard')} className="cursor-pointer flex items-center gap-2.5 group"><div className="w-8 h-8 rounded-lg bg-stone-900 text-white flex items-center justify-center font-bold text-sm tracking-tight transition-transform group-hover:scale-105">27</div><div><h1 className="font-semibold text-stone-900 text-sm md:text-base leading-tight tracking-tight">{tZen('2027 公考备考工作台',zenMode)}</h1><p className="text-[11px] text-stone-600 font-mono hidden sm:block">{zenMode?'Workstation & Personal Knowledge':'Guangdong & National Civil Service Track'}</p></div></div><div className="hidden lg:flex items-center gap-2 text-xs">{gd&&<div className="px-2.5 py-1 rounded-full bg-amber-50/80 border border-amber-200/70 text-amber-900 flex items-center gap-1.5 font-medium"><Calendar className="w-3 h-3 text-amber-600"/><span>{tZen('广东省考',zenMode)}</span><span className="font-bold text-amber-950 font-mono">{gd.daysLeft}</span><span className="text-[11px] text-amber-700">天</span></div>}{national&&<div className="px-2.5 py-1 rounded-full bg-blue-50/80 border border-blue-200/70 text-blue-900 flex items-center gap-1.5 font-medium"><Calendar className="w-3 h-3 text-blue-600"/><span>{tZen('国考副省级',zenMode)}</span><span className="font-bold text-blue-950 font-mono">{national.daysLeft}</span><span className="text-[11px] text-blue-700">天</span></div>}</div></div><div className="flex items-center gap-2 md:gap-3">{timerState.status!=='idle'&&<div onClick={()=>setActiveTab('timer')} className={`flex items-center gap-2 px-3 py-1.5 rounded-lg border text-xs font-mono cursor-pointer transition-all ${timerState.status==='running'?'bg-emerald-50 border-emerald-300 text-emerald-800 shadow-sm':'bg-amber-50 border-amber-300 text-amber-800'}`}><span className="relative flex h-2 w-2">{timerState.status==='running'&&<span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"/>}<span className={`relative inline-flex rounded-full h-2 w-2 ${timerState.status==='running'?'bg-emerald-600':'bg-amber-500'}`}/></span><span className="font-semibold">{fmt(timerState.elapsedSec)}</span><span className="text-stone-600 hidden sm:inline">[{timerState.currentModule}]</span><button onClick={e=>{e.stopPropagation();timerState.status==='running'?pauseTimer():resumeTimer()}} className="p-1 hover:bg-stone-200/60 rounded">{timerState.status==='running'?<Pause className="w-3 h-3 text-stone-700"/>:<Play className="w-3 h-3 text-stone-700 fill-stone-700"/>}</button></div>}<button id="btn-zen-mode" onClick={toggleZenMode} className={`px-2.5 py-1.5 rounded-lg text-xs font-medium flex items-center gap-1.5 transition-colors border ${zenMode?'bg-emerald-50 text-emerald-800 border-emerald-300 shadow-xs':'bg-stone-50 text-stone-600 border-stone-200 hover:text-stone-900 hover:bg-stone-100'}`}>{zenMode?<><EyeOff className="w-3.5 h-3.5 text-emerald-600"/><span className="hidden sm:inline">隐蔽中</span></>:<><Eye className="w-3.5 h-3.5 text-stone-600"/><span className="hidden sm:inline">隐蔽 (Alt+Z)</span></>}</button><button id="btn-header-coach" onClick={openAi} className="px-2.5 py-1.5 rounded-lg text-xs font-medium flex items-center gap-1.5 text-indigo-700 bg-indigo-50/80 hover:bg-indigo-100 border border-indigo-200/80 transition-colors"><Sparkles className="w-3.5 h-3.5 text-indigo-600"/><span className="hidden sm:inline">{tZen('AI 全局助手',zenMode)}</span></button><button id="btn-shortcuts" onClick={onOpenShortcuts} className="p-2 rounded-lg text-stone-600 hover:text-stone-800 hover:bg-stone-100 transition-colors"><HelpCircle className="w-4 h-4"/></button></div></header>;
};
