import React,{useMemo,useState} from 'react';
import {Play,Pause,Square,EyeOff,Eye,HelpCircle,Calendar,Sparkles,Menu,Timer,ChevronDown} from 'lucide-react';
import {useApp} from '../../context/AppContext';
import {tZen} from '../../utils/zen';

const fmt=(sec:number)=>`${Math.floor(sec/3600).toString().padStart(2,'0')}:${Math.floor((sec%3600)/60).toString().padStart(2,'0')}:${(sec%60).toString().padStart(2,'0')}`;
const localDate=(d:Date)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
const durationLabel=(sec:number)=>sec<3600?`${Math.round(sec/60)} 分钟`:`${(sec/3600).toFixed(1)} 小时`;

export const Header:React.FC<{onOpenShortcuts:()=>void;onOpenSidebar?:()=>void}>=({onOpenShortcuts,onOpenSidebar})=>{
  const{exams,timerState,startTimer,pauseTimer,resumeTimer,stopTimer,sessions,setActiveTab,zenMode,toggleZenMode}=useApp();
  const[gd,national]=exams;const[showTimer,setShowTimer]=useState(false);const openAi=()=>window.dispatchEvent(new CustomEvent('gongkao:open-ai'));
  const stats=useMemo(()=>{const now=new Date();const today=localDate(now);const day=(now.getDay()+6)%7;const monday=new Date(now);monday.setDate(now.getDate()-day);const weekStart=localDate(monday);const todaySec=sessions.filter(s=>s.date===today).reduce((a,s)=>a+Number(s.durationSec||0),0);const weekSec=sessions.filter(s=>s.date>=weekStart&&s.date<=today).reduce((a,s)=>a+Number(s.durationSec||0),0);return{todaySec,weekSec}},[sessions]);
  const primary=async()=>{if(timerState.status==='idle')await startTimer();else if(timerState.status==='running')await pauseTimer();else await resumeTimer()};
  return <header id="app-header" className="sticky top-0 z-30 h-16 border-b border-[#111820] bg-[#24292f] px-3 shadow-[0_2px_10px_rgba(17,24,32,0.22)] md:px-5"><div className="flex h-full items-center justify-between gap-4"><div className="flex min-w-0 items-center gap-3">{onOpenSidebar&&<button onClick={onOpenSidebar} className="md:hidden rounded-md p-2 text-[#d8dee4] hover:bg-[#30363d]"><Menu className="h-5 w-5"/></button>}<button onClick={()=>setActiveTab('dashboard')} className="group flex min-w-0 items-center gap-3 text-left"><span className="flex h-9 w-9 flex-shrink-0 items-center justify-center rounded-lg bg-stone-900 text-sm font-bold text-white ring-1 ring-white/20 transition-transform group-hover:-translate-y-0.5">27</span><span className="min-w-0"><span className="block truncate text-sm font-semibold tracking-tight text-white md:text-[15px]">{tZen('2027 公考备考工作台',zenMode)}</span><span className="hidden truncate text-[10px] font-medium tracking-wide text-[#9da7b1] sm:block">{zenMode?'Workstation & Personal Knowledge':'PERSONAL CIVIL SERVICE COACH'}</span></span></button><div className="hidden xl:flex items-center gap-1.5">{gd&&<span className="inline-flex items-center gap-1.5 rounded-md bg-[#3d3210] px-2.5 py-1 text-[10px] font-semibold text-[#ffd866]"><Calendar className="h-3 w-3"/>{tZen('广东省考',zenMode)} <b className="font-mono">{gd.daysLeft}</b> 天</span>}{national&&<span className="inline-flex items-center gap-1.5 rounded-md bg-[#102f55] px-2.5 py-1 text-[10px] font-semibold text-[#79b8ff]"><Calendar className="h-3 w-3"/>{tZen('国考副省级',zenMode)} <b className="font-mono">{national.daysLeft}</b> 天</span>}</div></div><div className="flex items-center gap-1.5 md:gap-2"><div className="relative"><button id="btn-compact-timer" onClick={()=>setShowTimer(v=>!v)} className={`relative inline-flex items-center gap-1.5 rounded-md px-2.5 py-2 text-xs font-semibold transition-all ${timerState.status==='running'?'bg-[#173d28] text-[#79d99a] ring-1 ring-[#2d7a4b]':timerState.status==='paused'?'bg-[#4b3b0c] text-[#ffd866] ring-1 ring-[#806a1b]':'bg-[#30363d] text-[#d8dee4] ring-1 ring-[#444c56] hover:bg-[#3b424b]'}`}><Timer className="h-4 w-4"/><span className="hidden lg:inline">{timerState.status==='idle'?'专注':fmt(timerState.elapsedSec)}</span>{timerState.status!=='idle'&&<span className={`h-1.5 w-1.5 rounded-full ${timerState.status==='running'?'bg-emerald-400':'bg-amber-400'}`}/>}<ChevronDown className="hidden h-3 w-3 opacity-60 lg:block"/></button>{showTimer&&<div id="compact-timer-popover" className="absolute right-0 top-12 z-50 w-80 overflow-hidden rounded-xl bg-white shadow-[0_28px_80px_rgba(17,24,32,0.28)] ring-1 ring-[#a8b3bf]"><div className="bg-[#dce9f7] p-5"><div className="flex items-start justify-between gap-3"><div><div className="text-[10px] font-semibold tracking-[0.12em] text-[#49515a]">FOCUS TIMER</div><div className="mt-1 font-mono text-3xl font-bold tracking-tight text-[#111820]">{fmt(timerState.elapsedSec)}</div><div className="mt-1 text-[10px] text-[#59636e]">{timerState.currentModule} · {timerState.currentActivity}</div></div><span className={`rounded-md px-2.5 py-1 text-[9px] font-semibold ${timerState.status==='running'?'bg-[#ccebd5] text-[#123d20]':timerState.status==='paused'?'bg-[#fff0b8] text-[#523f00]':'bg-[#dfe4ea] text-[#49515a]'}`}>{timerState.status==='running'?'进行中':timerState.status==='paused'?'已暂停':'未开始'}</span></div></div><div className="p-4"><div className="grid grid-cols-2 gap-3"><div className="rounded-lg bg-[#e9edf1] p-3"><div className="text-[9px] text-[#59636e]">今日累计</div><div className="mt-1 text-sm font-bold text-[#111820]">{durationLabel(stats.todaySec)}</div></div><div className="rounded-lg bg-[#e9edf1] p-3"><div className="text-[9px] text-[#59636e]">本周累计</div><div className="mt-1 text-sm font-bold text-[#111820]">{durationLabel(stats.weekSec)}</div></div></div><div className="mt-3 flex gap-2"><button onClick={primary} className="flex-1 rounded-md bg-[#0969da] px-3 py-2.5 text-xs font-semibold text-white hover:bg-[#0758b7]">{timerState.status==='running'?<><Pause className="mr-1.5 inline h-3.5 w-3.5"/>暂停</>:<><Play className="mr-1.5 inline h-3.5 w-3.5"/>{timerState.status==='paused'?'继续':'开始'}</>}</button>{timerState.status!=='idle'&&<button onClick={async()=>{await stopTimer();setShowTimer(false)}} className="rounded-md bg-[#ffd9d6] px-3 py-2.5 text-xs font-semibold text-[#8d2228]"><Square className="mr-1.5 inline h-3.5 w-3.5"/>结束</button>}</div></div></div>}</div><button id="btn-zen-mode" onClick={toggleZenMode} className={`rounded-md p-2.5 transition-colors ${zenMode?'bg-[#0d1117] text-[#f0f6fc] ring-1 ring-[#58a6ff]':'bg-[#30363d] text-[#d8dee4] ring-1 ring-[#444c56] hover:bg-[#3b424b]'}`} title="隐藏模式：深色界面 (Alt+Z)">{zenMode?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button><button id="btn-header-coach" onClick={openAi} className="inline-flex items-center gap-1.5 rounded-md bg-[#5f3dc4] px-3 py-2 text-[11px] font-semibold text-white ring-1 ring-[#7c5ce1] hover:bg-[#5334b0]"><Sparkles className="h-3.5 w-3.5"/><span className="hidden sm:inline">{tZen('AI 助手',zenMode)}</span></button><button id="btn-shortcuts" onClick={onOpenShortcuts} className="rounded-md p-2.5 text-[#c1c8d0] hover:bg-[#30363d]"><HelpCircle className="h-4 w-4"/></button></div></div></header>;
};